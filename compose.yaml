services:
  filebrowser:
    container_name: filebrowser
    image: ${FILEBROWSER_IMAGE:-ghcr.io/cryptnetworks/filebrowser:latest}
    restart: unless-stopped
    networks:
      - filebrowser
    ports:
      - "8000:80"
    volumes:
      - filebrowser_data:/srv
      - filebrowser_database:/database
      - filebrowser_config:/config
    environment:
      FB_REDIS_CACHE_URL: redis://default:filebrowser@redis:6379 # Use rediss:// for TLS.
    depends_on:
      - redis

  redis:
    container_name: redis
    image: redis:latest
    restart: unless-stopped
    networks:
      - filebrowser
    command:
      - sh
      - -c
      - |
        cat > /tmp/users.acl <<EOF
        user default on >filebrowser ~* +@all
        EOF
        redis-server --aclfile /tmp/users.acl

networks:
  filebrowser:

volumes:
  filebrowser_data:
  filebrowser_database:
  filebrowser_config:
